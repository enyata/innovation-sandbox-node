const wallet = require('./Wallet');
const retail = require('./Retail');
const utilities = require('./Utilities');
const enrollees = require('./Enrollees');
const main = require('./Main');
const clients = require('./Clients');

module.exports = {
    wallet,
    retail,
    utilities,
    enrollees,
    clients,
    main,
};
